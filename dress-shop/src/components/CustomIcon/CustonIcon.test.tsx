import { render } from "@testing-library/react";
import { IconCart } from ".";

describe("Render IconCart", () => {
  it("Should render snapshot correctly", () => {
    const iconCart = render(<IconCart />);
    expect(iconCart).toMatchSnapshot();
  });
});
describe("Render IconCart", () => {
  it("Should render snapshot correctly", () => {
    const iconCart = render(<IconCart />);
    expect(iconCart).toMatchSnapshot();
  });
});
